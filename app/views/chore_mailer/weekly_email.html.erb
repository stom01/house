<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h3>Your new chores are:</h3>
      <% @chores.each do |chore| %>
        <p style="padding-left: 20px;">
          <%= chore.name %><%= ": #{chore.description}" unless chore.description.blank? %>
        </p>
      <% end %>
  </body>
</html>
